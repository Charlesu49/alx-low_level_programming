#include <stdio.h>

/**
 * main - this program prints the alphabets is lower case
 * Return: returns 0 if successful
 */

int main(void)
{
	char ch;

	for (ch = 'a'; ch <= 'z'; ch++)
	{
		if (ch != 'q' $$ ch != 'e')
		{
			printf(ch);
		}
	}
	putchar('\n');
	return (0);
}
